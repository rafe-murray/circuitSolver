name: frontend
description: Frontend for Circuit Solver
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  http: ^0.13.6
  drift: ^2.21.0
  sqlite3_flutter_libs: ^0.6.0
  path_provider: ^2.1.0
  path: ^1.9.0
  protobuf: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  build_runner: ^2.4.6
  drift_dev: ^2.21.0
  protoc_plugin: ^20.0.0

flutter:
  uses-material-design: true
