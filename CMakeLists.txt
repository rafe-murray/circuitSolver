cmake_minimum_required(VERSION 4.0)
project(circuitSolver)
find_package(Ceres REQUIRED)
    include_directories(${CERES_INCLUDE_DIRS})
add_executable(solver main.cpp math/expression.cpp circuitGraph.cpp)
target_compile_options(solver PRIVATE -std=c++17 -c -g)
# target_compile_options(solver PRIVATE -std=c++17 -c -g -Wall -Wextra -pedantic)
add_link_options(-std=c++17 -lnlopt)
# add_link_options(-std=c++17 -lpthread -lm -lnlopt)
find_library(CERES libceres.dylib)
    target_link_libraries(solver ${CERES_LIBRARIES})
